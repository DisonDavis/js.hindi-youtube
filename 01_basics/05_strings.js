const name = "Dison"
const repoCount = 50

// console.log(name + repoCount + " Value")

// console.log(`Hello my name is ${name} and my rep count is ${repoCount}`) //string interpolation


console.log(name[0])
console.log(name.toUpperCase())

const gameName = new String("DisonTD") // intellisence will work, actually both name and gameName are same.

